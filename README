docbook2odf
------------------------------------------
Docbook2odf is a toolkit that automaticaly converts DocBook XML to OASIS 
OpenDocument (ODF, the ISO standardized format used for texts, 
spreadsheets and presentations). Conversion is based on a XSLT which 
makes it easy to convert DocBook->ODF, ODT, ODS and ODP as all these 
documents are XML based.

Runtime requirements:
  perl-5.10.0-5

Build requirements:
  cygport-0.9.10-1

Canonical website:
  http://open.comsultia.com/docbook2odf/

Canonical download:
  http://open.comsultia.com/docbook2odf/download

-------------------------------------------

Build instructions:
  unpack docbook2odf-0.244-X-src.tar.bz2
    if you use setup to install this src package, it will be
	 unpacked under /usr/src automatically
  cd /usr/src
  cygport ./docbook2odf-0.244-X.cygport all

This will create:
  /usr/src/docbook2odf-0.244-X-src.tar.bz2
  /usr/src/docbook2odf-0.244-X.tar.bz2

For a list of files included in the binary package, run from the 
command-line: 'cygcheck -l docbook2odf'.

-------------------------------------------

Port Notes:

----- version 0.244-1 -----
Initial release for Cygwin 1.7.


Cygwin port maintained by:
http://sourceware.org/cygwinports/
